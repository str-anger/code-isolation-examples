[project]
name = "isolate-docker-demo"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    "numpy>=2.3,<2.4",
]

[tool.uv]
# Kept standalone so this folder can be shared and built on its own.
package = false
