[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
# The caret is the whole lesson: ^1.26 means >=1.26,<2.0.0
python = ">=3.12,<3.13"
numpy = "^1.26"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0"
