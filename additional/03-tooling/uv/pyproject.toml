[project]
name = "isolate-uv-demo"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "numpy>=1.26,<2",
]

[dependency-groups]
dev = [
    "pytest>=8",
]

[tool.uv]
package = false
